{
  "scripts": {
    "dev": "npm run dev --prefix frontend/expense-tracker",
    "build": "npm run build --prefix frontend/expense-tracker",
    "preview": "npm run preview --prefix frontend/expense-tracker"
  }
}
